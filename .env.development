# Local development (npm run dev).
# Vite "/api" ko local Node server (localhost:3001) par proxy karta hai.
VITE_API_URL=/api

# Local me real-time SSE on rehti hai (Node backend support karta hai).
VITE_REALTIME=true
