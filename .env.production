# Live / Production build (npm run build) — Hostinger shared hosting.
# Same domain: frontend root par, PHP API "api/" (relative) par.
VITE_API_URL=api/

# Shared hosting SSE nahi rok sakta — rev-polling (~4s) se multi-device sync.
VITE_REALTIME=false
