// OPTIONAL runtime override — editable on the server WITHOUT rebuilding.
// Khaali ('') chhodo to env (.env.development / .env.production ka VITE_API_URL)
// wala URL use hoga. Sirf tab bharo jab live par API URL build se alag karna ho
// (e.g. Hostinger par API kisi aur path/domain par ho). Example: 'api/'
window.DAALROTI_CONFIG = {
  API_URL: '',
};
