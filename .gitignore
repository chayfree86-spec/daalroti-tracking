# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
*.local

# dist/ is COMMITTED so Hostinger git-deploy (docroot = dist/) gets the build.
# But DB credentials must NEVER go to git — server par config.php set rehta hai
# aur git pull use chhuega nahi (ignored). Build creds ko dist/api/config.php me
# copy karta hai (local), par woh commit nahi hoga.
api/config.php
dist/api/config.php

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.env
